{
  "name": "hw-game",
  "version": "0.0.1",
  "description": "Hello World game",
  "private": true,
  "scripts": {
    "compile": "rm -rf dist && webpack && cp src/*.html dist",
    "start": "npm run compile && open -a \"Google Chrome\" dist/index.html && npm run watch",
    "watch": "onchange src/**/* -- npm run compile"
  },
  "author": "Ben Pinhorn",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "ts-loader": "^4.4.2",
    "typescript": "^2.9.2",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0",
    "onchange": "^4.1.0"
  }
}
